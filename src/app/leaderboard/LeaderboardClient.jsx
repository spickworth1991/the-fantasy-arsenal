"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import Navbar from "../../components/Navbar";
import BackgroundParticles from "../../components/BackgroundParticles";
import { accountAvatar, useArsenalAccount } from "../../context/ArsenalAccountContext";

const pct=(record)=>(Number(record?.wins||0)+Number(record?.ties||0)*.5)/Math.max(1,Number(record?.wins||0)+Number(record?.losses||0)+Number(record?.ties||0))*100;
function Panel({children,className=""}){return <section className={`rounded-[28px] border border-white/10 bg-gradient-to-b from-slate-900/95 to-slate-950/90 ${className}`}>{children}</section>;}

export default function LeaderboardClient(){
  const {account,isConnected,refreshRecord}=useArsenalAccount();
  const [rows,setRows]=useState([]);
  const [season,setSeason]=useState(new Date().getFullYear());
  const [loading,setLoading]=useState(true);
  const [message,setMessage]=useState("");
  const load=async()=>{setLoading(true);try{const response=await fetch("/api/arsenal/leaderboard",{cache:"no-store"});if(!response.ok)throw new Error(await response.text());const result=await response.json();setRows(result.accounts||[]);setSeason(result.season||new Date().getFullYear());}catch(error){setMessage(error.message||"Leaderboard unavailable.");}finally{setLoading(false);}};
  useEffect(()=>{load();},[]);
  const refresh=async()=>{setMessage("");try{await refreshRecord();await load();setMessage("Your verified record and leaderboard position were refreshed.");}catch(error){setMessage(error.message||"Record refresh failed.");}};
  return <main className="min-h-screen text-white"><BackgroundParticles/><Navbar pageTitle="Manager Leaderboard"/><div className="mx-auto max-w-6xl px-4 pb-20 pt-20"><header className="overflow-hidden rounded-[34px] border border-emerald-300/15 bg-[radial-gradient(circle_at_88%_0%,rgba(16,185,129,.2),transparent_38%),radial-gradient(circle_at_5%_100%,rgba(139,92,246,.16),transparent_34%),linear-gradient(145deg,rgba(15,23,42,.98),rgba(2,6,23,.96))] p-5 sm:p-8"><div className="text-[10px] font-bold uppercase tracking-[.28em] text-emerald-200/60">Verified portfolio performance</div><h1 className="mt-2 text-3xl font-black sm:text-5xl">Arsenal Manager Leaderboard</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-white/48">Current-season records are rebuilt directly from public Sleeper rosters. Rankings use combined winning percentage across every verified league, followed by wins and points scored.</p><div className="mt-5 flex flex-wrap gap-2"><span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-xs text-white/55">{season} season</span><span className="rounded-full border border-white/10 bg-white/[0.04] px-3 py-2 text-xs text-white/55">{rows.length} eligible managers</span>{isConnected?<button type="button" onClick={refresh} className="rounded-full bg-emerald-300/10 px-4 py-2 text-xs font-black text-emerald-100">Refresh my record</button>:null}</div></header>{message?<div className="mt-4 rounded-2xl border border-cyan-300/15 bg-cyan-300/[0.05] p-4 text-sm text-cyan-100">{message}</div>:null}{loading?<div className="mt-5 rounded-2xl bg-white/[0.04] p-5 text-sm text-white/45">Loading verified records...</div>:<div className="mt-5 space-y-3">{rows.map((row,index)=><Link key={row.accountId} href={`/managers/${row.accountId}`} className="block transition hover:-translate-y-0.5"><Panel className={`p-4 ${row.accountId===account?.accountId?"border-cyan-300/25":""}`}><div className="grid grid-cols-[46px_52px_minmax(0,1fr)_72px] items-center gap-3 sm:grid-cols-[54px_58px_minmax(0,1fr)_90px_90px_90px]"><div className={`grid h-11 w-11 place-items-center rounded-xl text-sm font-black ${index<3?"bg-amber-300/10 text-amber-100":"bg-white/[0.04] text-white/45"}`}>#{index+1}</div><div className="grid h-12 w-12 place-items-center overflow-hidden rounded-2xl border border-white/10 bg-black/20"><img src={accountAvatar(row)} alt="" className="h-9 w-9 object-contain"/></div><div className="min-w-0"><div className="truncate font-black">{row.displayName}</div><div className="mt-1 truncate text-[10px] text-white/32">@{row.sleeperUsername}{row.favoriteTeam?` · ${row.favoriteTeam}`:""} · {row.fantasyStyle}</div></div><div className="text-right"><div className="text-xl font-black text-emerald-100">{pct(row.record).toFixed(1)}%</div><div className="text-[8px] uppercase text-white/25">Win rate</div></div><div className="hidden text-right sm:block"><div className="font-black">{row.record.wins}-{row.record.losses}{row.record.ties?`-${row.record.ties}`:""}</div><div className="text-[8px] uppercase text-white/25">Record</div></div><div className="hidden text-right sm:block"><div className="font-black">{row.record.leagues}</div><div className="text-[8px] uppercase text-white/25">Leagues</div></div></div></Panel></Link>)}{!rows.length?<Panel className="p-8 text-center"><h2 className="text-xl font-black">The leaderboard is ready</h2><p className="mt-2 text-sm text-white/40">Eligible profiles appear after their manager refreshes a verified record and enables leaderboard visibility.</p></Panel>:null}</div>}</div></main>;
}
